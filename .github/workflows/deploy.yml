name: deploy

on:
  push:
    branches: [ deploy ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: deploy
    steps:
      - name: Hello
        run: echo ${{ secrets.SECRET_TEST }}
      - run: ./gradlew assembleDebug